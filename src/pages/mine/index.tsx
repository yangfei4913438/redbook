import React from 'react';
import { Text, View } from 'react-native';

const Mine = () => {
  return (
    <View className="w-full h-full bg-white justify-center items-center">
      <Text className="text-3xl font-bold text-gray-500">This is Mine Page.</Text>
    </View>
  );
};

export default Mine;
